// Function types

let unnamed: () => string;

// Named and anonymous functions
// Named functions
function namedFunction(param: string) {
    
}

// Anonymous functions
let greet = function(name: string): string {
    return "Hi" + name;
};

